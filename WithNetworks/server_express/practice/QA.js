const express = require("express")
const router = express.Router()
const db = require("")

router.get("/QA",(req,res) => {
    const data = null
    //const db = ~~~~~~ //db의 데이터를 받는다
    const rowNum = data.length
    const 
})

